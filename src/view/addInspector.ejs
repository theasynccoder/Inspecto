<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Inspector – FSSAI Inspector Hub</title>
    <link rel="stylesheet" href="/css/addInspector.css" />
    <script src="https://unpkg.com/lucide@latest"></script>
  </head>
  <body>
    <%- include('partials/header1') %>

    <div class="add-container">
      <!-- Header Section -->
      <div class="add-header">
        <div>
          <h1>Add New Inspector</h1>
          <p>Fill in the details to register a new inspector under your zone</p>
        </div>
        <div class="assignment">
          <i data-lucide="user-plus" class="lucide-icon green"></i>
          <span>Zone: <%= zone %></span>
        </div>
      </div>

      <% if (success) { %>
      <div class="success-message">✅ Inspector added successfully!</div>
      <% } %>

      <!-- Form Card -->
      <div class="card">
        <h2>Inspector Details</h2>
        <p class="subtitle">
          Provide inspector's credentials and region assignment.
        </p>

        <form action="/admin/inspectors/add" method="POST" class="add-form">
          <div class="form-grid">
            <div>
              <label for="name">Full Name *</label>
              <input
                type="text"
                name="name"
                required
                placeholder="Inspector Name"
              />
            </div>

            <div>
              <label for="email">Email *</label>
              <input
                type="email"
                name="email"
                required
                placeholder="inspector@example.com"
              />
            </div>

            <div>
              <label for="phone">Phone Number *</label>
              <input
                type="text"
                name="phone"
                required
                placeholder="+91-XXXXXXXXXX"
              />
            </div>

            <div>
              <label for="region">Region *</label>
              <input
                type="text"
                name="region"
                required
                placeholder="Assigned region (e.g., Indiranagar)"
              />
            </div>

            <div>
              <label for="password">Password *</label>
              <input
                type="password"
                name="password"
                required
                placeholder="Create password"
              />
            </div>
          </div>

          <div class="assignment-info">
            <strong>Assignment Zone</strong>
            <p><%= zone %></p>
          </div>

          <div class="form-actions">
            <a href="/admin/inspectors" class="btn-outline">← Cancel</a>
            <button type="submit" class="btn-black">Add Inspector</button>
          </div>
        </form>
      </div>
    </div>

    <script>
      lucide.createIcons();
    </script>
  </body>
  <%- include('partials/chatbot') %>
</html>
