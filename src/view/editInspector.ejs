<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Inspector – FSSAI Inspector Hub</title>
  <link rel="stylesheet" href="/css/addInspector.css" />
  <script src="https://unpkg.com/lucide@latest"></script>
</head>
<body>

<%- include('partials/header1') %>

<div class="add-container">

  <!-- Header Section -->
  <div class="add-header">
    <div>
      <h1>Edit Inspector</h1>
      <p>Update the details for this inspector below.</p>
    </div>
    <div class="assignment">
      <i data-lucide="user-cog" class="lucide-icon green"></i>
      <span>ID: <%= inspector.id %></span>
    </div>
  </div>

  <!-- Form Card -->
  <div class="card">
    <h2>Inspector Details</h2>
    <p class="subtitle">Make necessary changes and click update.</p>

    <form action="/admin/inspectors/edit/<%= inspector.id %>" method="POST" class="add-form">
      <div class="form-grid">
        <div>
          <label for="name">Full Name *</label>
          <input type="text" id="name" name="name" value="<%= inspector.name %>" required>
        </div>

        <div>
          <label for="email">Email *</label>
          <input type="email" id="email" name="email" value="<%= inspector.email %>" required>
        </div>

        <div>
          <label for="phone">Phone Number *</label>
          <input type="text" id="phone" name="phone" value="<%= inspector.phone %>" required>
        </div>

        <div>
          <label for="region">Region *</label>
          <input type="text" id="region" name="region" value="<%= inspector.region %>" required>
        </div>
      </div>

      <div class="assignment-info">
        <strong>Zone</strong>
        <p><%= inspector.zone || 'Unknown' %></p>
      </div>

      <div class="form-actions">
        <a href="/admin/inspectors" class="btn-outline">← Cancel</a>
        <button type="submit" class="btn-black">Update Inspector</button>
      </div>
    </form>
  </div>
</div>

<script>
  lucide.createIcons();
</script>
</body>
<%- include('partials/chatbot') %>
</html>
